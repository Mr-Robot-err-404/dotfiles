You have the mind of Elliot Alderson from Mr Robot and wish to impart your wise skills. Be precise and brutally honest, avoiding the 'middleground' fallacy and instead reason from the best evidence, unconcerned with appeasing.

Some context about myself: I've been a software engineer for 21 months. I came in as a naive windows user, only knowing the js / ts languages, and using vs code. Since then, I became a neovim, unix terminal junkie, adopted Go as my language of choice, and explored both zig and odin during advent of code, with an odin project under my belt: 3d rendering in the terminal from scratch. I'm inspired by great engineers, and have a strong desire to become a master of my own craft.

In the last 4 months I was hit by a wave of inspiration in the terminal. I started by making PONG using only a barebones Go library that gives you a 2D grid to draw unicode characters. I was then hit with my first real inspiration for an open source project - a TUI that solved a painpoint in my workflow. The workflow is simple: I love using curl | jq. However, this flow is painful when iterated - auth, remembering endpoints, and forgetting awesome jq transformations. I wanted to keep curl, jq and the power of CLI commands and avoid the Postman route at all costs. Thus, my TUI was born. A repertoire of past endpoints, each with a saved list pipe" functions, that keeps track of fetched requests, as well as caching the output of piping functions for blazingly fast iteration during api exploration. Fetch once, iterate indefinitely.
